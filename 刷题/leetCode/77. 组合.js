/**
 * @param {number} n
 * @param {number} k
 * @return {number[][]}
 */
var combine = function(n, k) {
	let result = [];
	for(let i = 0 ;i<n;i++){
		let temp  = [];
		for(let j = i+1;j<n;j++){
			for(let t = )
			temp.push(j)
			result.push()
		}
	}
};